#pragma warning (disable:4996)
#include<stdio.h>
#include<string.h>
#include<stdlib.h>

/*
int main()
{
    int tree[10005] = {0};

    int l, m;
    scanf("%d%d", &l, &m);
    for (int i = 0; i < m; i++)
    {
        int begin, end;
        scanf("%d%d", &begin, &end);

        for (int j = begin; j <= end; j++)
        {
            tree[j] = 1;
        }
    }
    int count = 0;
    for (int i = 0; i <= l; i++)
    {
        if (!tree[i])
            count++;
    }

    printf("%d", count);
    return 0;
}
*/